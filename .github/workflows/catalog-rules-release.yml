name: Catalog Rules Release

permissions:
  id-token: write
  contents: write

on:
  push:
    tags:
      - "catalog-rules-v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Set node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          registry-url: https://registry.npmjs.org
      - name: Install pnpm
        uses: pnpm/action-setup@v4
      - name: Setup
        run: npm i -g @antfu/ni
      - name: Install
        run: nci
      - name: Publish
        working-directory: packages/catalog-rules
        run: pnpm publish --access public --no-git-checks